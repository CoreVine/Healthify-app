import 'package:flutter/material.dart';

class DietDataScreen extends StatelessWidget {
  const DietDataScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
